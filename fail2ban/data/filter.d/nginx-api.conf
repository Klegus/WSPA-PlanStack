[Definition]
failregex = ^<HOST> .* "(GET|POST|HEAD)(?!/api/status).*" (403|404|429|444) .*$
            ^<HOST> .* "(GET|POST|HEAD)(?!/api/status).*" 401 .*$
ignoreregex = ^<HOST> .* "GET /api/status.*" .*$